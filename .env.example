# Hugging Face token — required for downloading model weights
# Get one at https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here

# Uncomment to disable torch.compile (faster startup, slightly slower inference)
# NO_TORCH_COMPILE=1
